# Raw Sources

这里保存原始来源。Codex 默认不修改本目录下的文件。

## 子目录

- `x/`: Obsidian Web Clipper 保存的 x.com 内容。
- `assets/`: 图片、截图、视频封面和其他附件。

## 使用建议

- x.com 推文、thread、article 和 profile 都保存到 `raw/x/`。
- 附件统一下载到 `raw/assets/`，避免远程链接失效。
- 原始文件可以保留 Web Clipper 的默认 frontmatter 和正文格式。

